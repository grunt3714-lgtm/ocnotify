[package]
name = "ocnotify"
version = "0.3.0"
edition = "2021"
description = "CLI wrapper that monitors subprocesses and sends smart progress notifications via OpenClaw"

[[bin]]
name = "ocnotify"
path = "src/main.rs"

[dependencies]
regex-lite = "0.1"
